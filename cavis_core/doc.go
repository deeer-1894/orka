// Package cavis_core is the protocol + SDK layer of the Cavis platform.
//
// It hosts the cross-module event protocol (messages), state/checkpoint
// abstractions, the agent runtime (RunContext/Middleware/Runner), a
// reconnecting WebSocket client, tracing primitives and the signed
// context-token security helpers.
package cavis_core
