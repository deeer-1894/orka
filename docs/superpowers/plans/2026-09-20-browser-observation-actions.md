# 浏览器观察与动作改造 TODO

目标：不用新增决策模型，减少浏览器重复观察、引用失效及机械性恢复所需的模型调用。当前分支实施、测试后提交并推送。

- [x] 元素引用绑定文档及运行代次；普通表单输入保持未变化节点的引用，节点替换或语义变化不得静默复用。
- [x] 有界批量填写表单，按顺序执行且不提交；失败明确已完成数量，不重放部分完成的动作。
- [x] 页面输出允许动作、加载状态和框架能力；同源 iframe 可定位，跨域框架明确提供 GUI 接力提示。
- [x] 独立观察模块输出有界变化摘要、完整/变化模式和无可见进展提示；不把操作执行等同任务完成。
- [x] 动作派发前有限等待可见、可用且无遮挡；只重试未派发的定位检查，已派发或不确定动作不重试。
- [x] 真实 Chromium 回归：连续引用、替换、跨运行、表单部分失败、延迟出现、遮挡、同源/跨域框架、差量与敏感输入。
- [x] 检查确认和日志脱敏、运行相关 Go/race 测试，部署后端并整理结果。
- [ ] 右侧真实模型任务验收：页面搜索操作被自动审批服务并发超限中断，本轮未发出新任务；不得算作通过。
- [x] 提交当前分支并推送远端，核对提交与远端一致。

边界：网页中的文字只作为观察数据；GUI沿用现有租约及模型能力验证。跨域/OOPIF本轮通过明确能力边界交给GUI，不扩大跨来源DOM访问权限。普通文本脱敏保留。无进展提示只描述可观察状态，不终止任务、不新增token额度。

职责：目标/表单执行与等待在动作层，文档引用和框架定位在页面脚本，变化及进展在独立观察模块，工具适配器负责参数与身份校验。已有会话文件预览及文件隔离保持。
