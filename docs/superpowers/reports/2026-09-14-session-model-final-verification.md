# 最终整合验收

2026-09-14，分支 `codex/long-task-optimization`。

## 已验证

- 全量 Go 测试与 `go vet`：`orka_core/...`、`orka_middleware/...`、`tools_server/...`、`orka_control_layer/...` 全部通过。
- Race 检查：modelsettings、service、api、pathsafe、security、tools server/tools 全部通过。
- 前端单元测试 46/46，独立浏览器回归 7/7，生产构建通过。现有大包体积提示仍存在。
- 后端与前端已重启，8088 `/health` 和 5174 首页均返回 200。工具镜像更新为本分支的会话隔离实现。
- `real@test.com` 注册及登录通过。两个实际会话验证了上传、目录、下载、其他会话不可见、越界与缺少会话上下文拒绝；浏览器验证文件夹导航、CSV、代码高亮和切换时异步结果隔离。
- 本地兼容服务实际收到 Auto 对应列表第一项；手动对应指定模型。两轮各包含聊天、标题、追问共 3 个调用，均保持选定模型。
- 显式未知模型未触发模型请求；改变配置后旧回答追问未触发请求。
- 右侧真实页面刷新后菜单只含 Auto 与模型名称，配置页没有主、次模型字段。
- 测试临时模型服务已停止，仅移除本次为测试账号创建且未被用户修改的临时配置，账号回到系统默认配置。

## 交叉审查

修复两个发现：跨会话模型请求共享限流；追问校验原配置版本。只读复查确认解决，客户端池活跃租约、空闲淘汰与定时器竞态保护未发现新的重要问题。

## 边界

厂商配置使用 OpenAI Chat Completions 兼容协议；真实厂商可用性仍取决于相应地址与有效密钥，本次联网调用使用可控本地服务。工作区提供会话根目录选择和文件 API 约束，Shell/Python 仍沿用已有容器挂载边界，不是独立 OS 沙箱。历史用户文件保留在旧位置，不作为新会话文件回退显示。
