# 长任务修复与独立验收

日期：2026-09-17。分支：`codex/long-task-optimization`。

## 测试范围

在右侧页面 `http://localhost:5174/` 的 `real@test.com` 账号中，使用 `glm-5.3-flash`，继续真实 SQLite 备份审计项目。会话：`8fd971a5ce2b506102d018b7`。原始任务见 [测试任务](2026-09-17-sqlite-audit-task.md)，修复前观察见 [基线报告](2026-09-17-sqlite-audit-results.md)。基线报告描述修复前状态，不代表本文部署后的结果。

所有破坏性边界检查使用独立临时测试数据库，未接触用户业务数据库。

## 已修改的 Orka 行为

| 问题 | 修改 | 验证 |
| --- | --- | --- |
| 测试失败被管道尾部成功命令掩盖 | 执行器使用 Bash fail-fast 与 pipefail；镜像明确包含 Bash | 失败管道回归、真实容器执行契约检查 |
| 进程完成被混同为需求验收通过 | 保存真实退出码、标准输出/错误、超时信息的独立执行记录；工作台明确区别执行和业务验收 | 正常、失败、不完整回执、账号隔离测试 |
| 测试结果对应旧版本代码 | 记录执行开始时已声明交付文件的 SHA-256，标记执行期间/之后版本不同或不可读取 | 修改、删除、恢复、大小边界、越界链接测试；右侧实际 ZIP 版本绑定 |
| 读取网页片段却声称全文 | 返回完整可读文本/截断/选取片段的范围信息，证据存储不再称全文 | HTML 提取、字节边界与章节测试 |
| Python API 章节定位偏移 | 将带锚点的 HTML 定义项纳入章节定位 | backup 定义定位回归 |
| 模型误认为临时目录/本机服务跨调用持久 | 工具说明明确临时路径、后台进程生命周期和代码沙箱与浏览器隔离 | 真实任务失败原因分析；未放宽隔离 |
| 终端生成的 ZIP/报告遗漏于会话文件入口 | 执行器返回有界文件变更元数据；前端核对文件存在后展示，失败进程产物也可找到 | 元数据、失败回执、会话隔离和下载浏览器测试 |
| SQLite 主文件、WAL、SHM 被误报重复 | 删除按文件名前缀猜重复的提示 | 文件浏览回归 |
| 文件太多挤占聊天 | 默认展示8个文件，可展开；展开区限高滚动，保留预览和下载 | 20文件浏览器检查，真实66文件会话确认 |
| 概览执行中显示0用量 | 当前运行直接读取持久化用量账本，保留在途/未知用量说明；错误不伪装成0 | 账本非0、终态计数仍0的浏览器回归 |
| 历史列表产生大量用量查询 | 当前运行持续更新；历史用量仅在用户展开明细时读取 | 20运行列表初始0次查询，展开后仅1次 |
| 新证据ID影响重复调用识别 | 重复检测只比较原始工具结果，之后追加唯一证据备注 | 第3次相同调用仍产生重复提醒 |

执行版本记录是有限范围的文件观测，不会证明命令实际读取了所有文件，也不会自动推断业务测试通过。单次最多记录64个已声明文件、总计8MiB，超出或不可读明确标记不完整；历史接口最多返回200条执行记录，保留截断标志。这些是读取与展示边界，不是任务 token 限额。

## 长任务中的实际回归

第一轮修复中，模型为了保存失败日志，将旧版本备份代码恢复进最终目录，最后交付包仍有“失败覆盖时删除已有备份”问题。

- 第一份修复包 SHA-256：`34e1264981407cc542af2733372177e14ea358a19da6a456ba67f93b252f4760`。
- 独立 CLI 检查：10/11通过；下载包自带26项测试实际1项错误。
- 已将失败事实反馈到右侧会话，并加强软件交付指导：不得为了重现历史失败覆盖最终源码；必须对最终归档的新解压副本复测，记录被测归档哈希。
- 第二轮真实修复 `run_49355c257416acb649d39f97` 完成，46次工具调用，用量账本679987 tokens；该值不是价格或付款金额。

最终包：`sqlite-audit-fixed.zip`，75600字节、33项文件。

SHA-256：`f8dcb206d480f0803177e7602054ee56c6423e5cbeeb550a6f57a7d1719548c8`。

## 最终包独立验证

| 检查 | 结果 |
| --- | --- |
| 下载包新解压后的自带测试 | 26/26通过，退出0 |
| 独立编写的CLI边界检查 | 11/11通过 |
| README四条命令按原样运行 | 全部退出0 |
| 实际浏览器勾选“只看异常” | 正常表格、样本、原始JSON隐藏；异常区保留；取消勾选恢复 |
| 包内原始研究证据 | 已包含 docs/evidence 文件 |
| 部署后右侧再次验证 | `run_a2b0c07c179599fc5eacfca2` 完成，真实26项测试通过 |
| 执行记录与交付版本关联 | 受保护记录包含上述ZIP SHA-256，退出0，无版本变化；工作台实际展示已核对 |

Orka自身的浏览器未能直接访问代码沙箱内的临时HTTP服务，因此模型按事实保留“页面交互未验证”。本次HTML实际交互检查由外部独立验收浏览器完成；二者不能混为一谈。

## Orka 回归和部署

- 后端 `service`、`api`、`browsertool` 测试通过。
- 工具服务 `runner`、`tools` 测试通过；真实Docker中隔离、退出码、超时与取消契约检查通过。
- 前端单元测试通过；hardening与会话文件浏览器回归52项通过。最后懒加载变更的3项相关专项回归通过（含新增历史用量按需查询用例）。
- 前端构建通过；仍有原有大包体提示。
- 本机后端、工具Docker及前端已部署并重启；8088健康检查、5174页面均HTTP 200。右侧页面刷新后运行新任务验证。
- 没有新增任务、会话或账号token限额。

## 仍需关注

1. 通用执行记录不能替代针对任务的独立语义验收；模型仍可能写出无效断言或遗漏边界。当前成果由外部独立检查兜底，不能宣称以后所有项目都会自动正确。
2. 未声明交付文件的执行记录没有文件版本关联；本轮观察到模型漏登记一次，明确补充要求后才登记成功。后续可完善文件交付契约，避免靠追问纠正。
3. 长上下文中仍出现重复确认、错误路径、只检查stdout而遗漏stderr等多余调用。没有通过限制token或截断任务掩盖这些问题。
4. 生成项目还有非阻断瑕疵：报告标题变量被表名循环覆盖，示例页面标题为orders；README对并发备份时间点的概括不应作为并发一致性的保证。这些不属于上述已通过的11项独立检查，不能算已修复。
5. 历史运行仍按执行段列出，暂停后恢复的同一逻辑任务会占多条记录；后续可按任务归并统计。

本机原始证据位于忽略目录 `.run/sqlite-audit-fix-observation/`（下载包、独立检查JSON、测试日志、HTML交互截图、部署后执行证据）。包含会话上下文的原始记录不作为公开仓库资料提交。
