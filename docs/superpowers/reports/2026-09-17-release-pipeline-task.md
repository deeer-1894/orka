继续维护本会话已实际运行的 SQLite 批量审计项目。以 sqlite-audit-batch.zip 为输入，在新的 sqlite-audit-release/ 目录工作，保留旧ZIP原字节，不从头重写业务模块。

请交付一个可重复运行的发布流程，解决上一轮“测试确实通过，但README承诺的日志没有随包交付、收尾改动后反复打包”的问题：

1. 先检查原ZIP的实际内容与README引用，记录确实缺失的日志或资料；区分交付承诺、历史记录和演示命令将生成的文件。不要为了消除提示凭空补造日志。
2. 仅用Python标准库实现独立的发布模块和命令行入口，按“在隔离副本执行完整测试并保存stdout/stderr与退出码→收集明确文件清单→生成ZIP→从最终ZIP新解压复跑→生成包外验证记录”的顺序执行。模块职责分开；业务审计代码尽量复用。发布文件清单固定排序，拒绝符号链接，排除旧ZIP、生成数据库、缓存及隐藏临时文件；保留真正需要的源码、文档、测试、证据、示例JSON/CSV/HTML和实际日志。不要仅用ls或文件存在证明测试通过。
3. ZIP打包必须有稳定元数据，相同输入内容重复打包得到相同字节。包内文件清单记录实际成员路径、大小和SHA-256，清单自身明确不参加自引用；包外验证记录记录被测最终ZIP哈希、实际命令、退出码、测试结果与未验证项，最终哈希不得写回包内造成循环。发布失败不能覆盖上一次成功的包和验证记录；输出不得覆盖任何源文件，包括硬链接别名。
4. 保留原52项测试，新增至少12项有意义的发布回归：缺少必需文件、路径含中文空格、重复打包字节一致、旧ZIP/缓存/临时数据库排除、包内清单逐项核验、符号链接、真实测试失败、失败保留上次成功交付、输出与输入冲突。使用临时测试夹具，不操作其他会话或真实用户数据库。修复发现的问题后再验证。
5. 更新README，让它的命令可以从新解压目录原样执行。保留历史14/26项日志原含义，本轮日志保存真实执行输出。README里每个承诺交付的日志都必须能在ZIP中找到；不把示例输出或未实测的HTML交互说成已验收。
6. 最终交付 sqlite-audit-release.zip 与 release-validation.md。先完成代码、文档及包内日志，再做一次最终打包和全新解压验收；有新的失败或内容改动才重跑，不为了换一段摘要反复执行相同测试。最终用Markdown可点击链接交付，说明实际通过数量和限制。

全过程自行执行，不需要重复已有行业/技术调研，不安装依赖。遇到环境限制用已有标准库解决；明确区分实际错误和推测原因。先给简短计划，然后完成实现与测试，不能只写计划或伪造验证记录。
