# SQLite 备份审计长任务复测

日期：2026-09-17。模型：glm-5.3-flash；工具：自动（不勾选）；高危操作确认：开启。

## 初始任务

请完成一个可实际使用的 SQLite 备份审计小项目，全部文件放在本会话的 sqlite-audit/ 目录。

先查阅 SQLite 官方文档和 Python sqlite3 官方文档，核实在线备份 API、WAL 模式下直接复制单个数据库文件的风险，以及 integrity_check 与 foreign_key_check 的区别。至少保留3个真实官方来源URL、抓取时间、关键原文摘录和适用限制；找不到的不要猜。

然后实现一个只用 Python 标准库、离线可运行的命令行程序，分离数据库检查、备份、报告和CLI模块，提供 demo、audit、backup 三个子命令。demo 创建明确标为测试夹具的订单数据库；audit 只读检查数据库完整性、外键违规、表和行数，输出JSON及可本地打开的HTML报告；backup 用在线备份接口，验证备份可读并记录SHA-256。不能把完整性检查通过说成业务数据一定正确。源数据库不得被修改，已有目标文件默认拒绝覆盖，失败不能留下看似成功的正式备份。

实际运行至少12项有意义的测试，覆盖正常数据库、外键违规、损坏文件、不存在的源文件、空数据库、Unicode及HTML转义、已有备份拒绝覆盖、备份失败清理、源文件不变和CLI失败退出码。别用永远通过的断言凑数，测试失败须修复，真实执行日志要保存。

写中文README和研究说明，演示一次建库、审计、备份及重新审计备份；最终打包源码、测试、说明、示例报告和证据为sqlite-audit.zip，并解压到独立目录按README重新运行，确认包可用。最终给出下载链接、真实测试数量和仍未验证的限制。不要安装第三方依赖，不要操作本项目之外的文件，不要只停在计划。

## 执行中追加

追加要求，请在当前任务内调整，不要从头重做：HTML报告需要“只看异常”开关；另外增加一个测试证明路径中含中文、空格、#和?的数据库能够审计及备份。最终摘要请单独说明这两项的实际验证结果。
